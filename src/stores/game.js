import { defineStore } from 'pinia';

let usegameStore = defineStore('game Store', {
    state: () => ({
        reappeareCode: 'None',
        gameCode: 'None',
    }),
    actions: {
        
    },
    getters: {
        
    }
});

export default usegameStore;